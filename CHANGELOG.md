## 0.1.0

* Initial release of `web_ui_plugin`.
* Enforces non-nullable `uid` constraints on `DataModel` implementations.
* Plug-and-play SaaS admin UI plugin framework for Flutter Web.
* Firebase-first adapters and repository models.
